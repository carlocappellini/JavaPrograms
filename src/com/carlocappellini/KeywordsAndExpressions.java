package com.carlocappellini;

public class KeywordsAndExpressions {
    public static void main(String[] args) {
        // a mile is equal to 1.609344
        // the data type does not form part of an expression , but anything else on the line is

        //valid expression
        // kilometers = (100 * 1.609344);
        //  by adding the data type we are creating a valid java statement
        double kilometers = (100 * 1.609344);

        int highScore = 50;

        if (highScore == 50) ;
        //(highScore == 50); is an expression, anything in the brackets
        System.out.println(" this is also an expression");

    }
}
